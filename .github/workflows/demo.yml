name: GitHub Actions Demo
on: [push]
jobs:
  main:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        name: checkout

      - uses: ./.github/actions/setup-qt
        id: x

      - run: echo "${{ steps.x.outputs.qt-path }}"
        shell: cmd
