name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          set PATH=C:\Miniconda;%PATH%
          python -m pip install mugideploy
          python -m mugideploy --help
        shell: cmd

